services:
  - type: web
    name: app-review-scraper
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run ui
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: "20"
